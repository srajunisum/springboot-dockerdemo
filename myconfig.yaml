apiVersion: apps/v1
kind: Deployment
metadata:
  name: SDD
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      run: SDD
  template:
    metadata:
      labels:
        run: SDD
    spec:
      containers:
      - name: SDD
        image: https://hub.docker.com/r/ajayk333/sdd
        resources:
          limits:
            memory: "5000Mi"
            cpu: "500m"
        ports:
          - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: bootapp-entrypoint
  namespace: default
spec:
  type: NodePort
  selector:
    run: SDD
  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 30003